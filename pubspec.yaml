name: latihan_image_supabase
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  # Baris-baris some_package dan another_package telah dihapus karena tidak ditemukan di pub.dev,
  # yang menyebabkan kesalahan "exit code 69" dan kegagalan parsing YAML.
  
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.12.0
  image_picker: ^1.2.1
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# Bagian lain dari file (termasuk flutter:) tidak diubah, 
# tetapi harus dipastikan indentasinya benar.
flutter:
  uses-material-design: true